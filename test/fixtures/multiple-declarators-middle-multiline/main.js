const Math = require('math'),
      // Ratio of a circle's circumference to its diameter.
      PI = Math.PI,
      // The largest positive number.
      Infinity = Math.Infinity,
      // Some circle's radius.
      radius = 9;

console.log(2 * PI * radius);
